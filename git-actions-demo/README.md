# Git + GitHub Actions Demo App (No Tailwind)

A sleek one-page React app built with **Vite** and **vanilla CSS** — great for demonstrating:
- Git basics (init, add, commit, branches)
- Pull requests and review
- GitHub Actions CI checks on push/PR

## Requirements
- Node.js 18+ recommended
- npm

## Setup

Install dependencies:

```bash
npm install